# WhisperTranscribe ユーザーマニュアル

ローカル PC 上で動作する音声・動画ファイル文字起こしツール。録音機が分割した複数ファイルをひとまとめにし、Whisper による文字起こしまでを1アプリで完結します。

---

## 目次

1. [このアプリでできること](#1-このアプリでできること)
2. [動作環境](#2-動作環境)
3. [インストール](#3-インストール)
4. [初回セットアップ](#4-初回セットアップ)
5. [GUI版の使い方](#5-gui版の使い方)
6. [CLI版（wtcli）の使い方](#6-cli版wtcliの使い方)
7. [モデルの選び方](#7-モデルの選び方)
8. [初期プロンプトの書き方](#8-初期プロンプトの書き方)
9. [入力フォーマットについて](#9-入力フォーマットについて)
10. [出力フォーマット](#10-出力フォーマット)
11. [ファイル保管場所](#11-ファイル保管場所)
12. [トラブルシューティング](#12-トラブルシューティング)
13. [Colab 半手動連携（無料 GPU 利用）](#13-colab-半手動連携無料-gpu-利用)

---

## 1. このアプリでできること

- 音声ファイル（WAV/MP3/M4A/FLAC/OGG等）・動画ファイル（MP4/MOV/MKV/AVI等）の**文字起こし**
- 録音機が30分単位などで分割した**複数ファイルを順序通りに連結**
- 録音レベルのばらつきを**ラウドネス正規化**で均す
- **32bit float WAV** など Whisper が直接読めない形式を自動変換
- モデル（精度と速度のトレードオフ）を GUI から**ダウンロード・切り替え**
- 出力は `.txt` / `.srt` / `.vtt` 形式
- インターネット不要で動作（初回のみ ffmpeg とモデルの取得のため接続が必要）

---

## 2. 動作環境

| 項目 | 要件 |
|---|---|
| OS | Windows 10/11 x64 |
| .NET | .NET 8 Desktop Runtime |
| CPU | x86/x64（AVX 非対応 CPU は自動で `CpuNoAvx` ランタイムが選ばれます） |
| RAM | 4 GB 以上（large 系モデルは 6 GB 以上推奨） |
| ディスク | 100 MB（アプリ本体）＋ モデル分（最小 77 MB 〜 最大 約3 GB） |
| 通信 | 初回 ffmpeg・モデル DL のみ |

---

## 3. インストール

### バイナリ利用の場合

GitHub Releases からビルド済み ZIP をダウンロードし、任意のフォルダに展開。

### ソースからビルドする場合

```powershell
git clone https://github.com/NamakeroBK/WhisperTranscribe.git
cd WhisperTranscribe
dotnet build -c Release
```

生成物:
- GUI: `bin\Release\net8.0-windows\WhisperTranscribe.exe`
- CLI: `TestRunner\bin\Release\net8.0\wtcli.exe`

---

## 4. 初回セットアップ

アプリは次の2つを必要としますが、**初回起動時に自動でダウンロード**されます。

1. **ffmpeg / ffprobe**（約100 MB）
   - 用途: 音声・動画のデコード、連結、正規化、リサンプリング
   - 取得元: gyan.dev の Release Essentials ビルド
   - 保存先: `%LOCALAPPDATA%\WhisperTranscribe\ffmpeg\`

2. **Whisper モデル**（77 MB 〜 約3 GB、選んだものだけ）
   - 用途: 文字起こしエンジン
   - 取得元: HuggingFace（ggerganov/whisper.cpp）
   - 保存先: `%LOCALAPPDATA%\WhisperTranscribe\models\`

> 一度ダウンロードすれば再取得は不要です。

---

## 5. GUI版の使い方

### 5-1. 起動と初回ダウンロード

1. `WhisperTranscribe.exe` を実行
2. 初回は ffmpeg のダウンロードを促すダイアログが出るので「はい」
3. 続けて右上の **「モデル管理...」** を開き、使いたいモデルを **「選択をダウンロード」**

### 5-2. 文字起こしの流れ

| 手順 | 操作 |
|---|---|
| ① | 音声/動画ファイルをウィンドウ中央の一覧に**ドラッグ&ドロップ**（または「追加...」/「フォルダ追加...」） |
| ② | 一覧の順序が連結順。必要なら **「上へ」「下へ」** で並び替え |
| ③ | 設定パネルで **モデル**・**言語**・**出力フォルダ**・**出力形式** を指定 |
| ④ | 必要なら **「初期プロンプト」** に例文を入力（精度向上に有効・[後述](#8-初期プロンプトの書き方)） |
| ⑤ | **「文字起こし開始」** を押す |
| ⑥ | 進捗バーとログを見ながら待機。完了後、出力フォルダに `.txt` / `.srt` / `.vtt` が生成される |

### 5-3. UI 各部の説明

```
┌─ 入力ファイル ──────────────────────────────┐
│ ファイル名 / 長さ / 形式 / 状態 / パス        │  ← 32bit float 検出など、各ファイルの中身を表示
│ [追加] [フォルダ追加] [上へ] [下へ] [削除] [全削除]│
└─────────────────────────────────────────────┘

┌─ 設定 ──────────────────────────────────────┐
│ モデル: [▼ ggml-small]   言語: [▼ 日本語]   出力フォルダ: [...] │
│ ☑ ラウドネス正規化  ☐ 高品質モード(beam=10)              │
│ ☑ .txt  ☑ .srt  ☐ .vtt   出力名: [transcript]          │
│ 初期プロンプト: [模範的な書き起こし例を入力 ...]          │
└─────────────────────────────────────────────┘

[ 進捗バー ───────────────────── ] [文字起こし開始] [中止]

[ ログ表示エリア ]
```

---

## 6. CLI版（wtcli）の使い方

バッチ処理・スクリプトからの呼び出し用にコマンドラインインターフェースも用意しています。

### 基本

```powershell
wtcli <input> [--model NAME] [--lang LANG] [--prompt TEXT] [--hq] [--no-norm] [--out DIR]
```

### よく使う例

```powershell
# 1ファイルを日本語・smallモデルで文字起こし
wtcli "C:\rec\meeting.m4a" --model small --lang ja

# 複数ファイルを連結して文字起こし
wtcli "C:\rec\part1.wav" "C:\rec\part2.wav" "C:\rec\part3.wav" --model large-v3-turbo

# 高品質モード + 初期プロンプト
wtcli "C:\rec\interview.mp3" --model large-v3 --hq `
      --prompt "本日のインタビューでは、新製品の開発経緯についてお話を伺います。山田取締役、田中エンジニアにご登壇いただいています。"

# 出力先を指定、ラウドネス正規化なし
wtcli "C:\rec\audio.wav" --model small --out "C:\transcripts" --no-norm
```

### オプション一覧

| オプション | 説明 | デフォルト |
|---|---|---|
| `--model NAME` | モデル名 (tiny / base / small / medium / large-v3 / large-v3-turbo 等) | `small` |
| `--lang LANG` | 言語コード (`ja` / `en` / `auto` 等) | `ja` |
| `--prompt TEXT` | initial_prompt（例文を渡す） | なし |
| `--hq` | 高品質モード (beam_size=10) | OFF |
| `--no-norm` | ラウドネス正規化を無効化 | 正規化ON |
| `--out DIR` | 出力フォルダ | 入力と同じ場所 |

### 環境変数による調整

| 変数 | 意味 | 例 |
|---|---|---|
| `WHISPER_RUNTIME` | ランタイム優先順（カンマ区切り） | `Cuda,Cpu` / `Cpu` / `CpuNoAvx` |

---

## 7. モデルの選び方

日本語精度・速度・サイズの目安です（CPU 推論時の体感）。

| モデル | サイズ | 速度 | 日本語精度 | 用途 |
|---|---|---|---|---|
| **tiny** | 77 MB | 最速 | △ | 試運転・速報 |
| **base** | 148 MB | 速い | △ | 内容把握 |
| **small** | 488 MB | 中 | ○ | **デイリー用途のバランス型（おすすめ）** |
| **medium** | 1.5 GB | 遅い | ◎ | 議事録など |
| **large-v3** | 3.0 GB | 最遅 | ◎◎ | 最高精度 |
| **large-v3-turbo** | 1.6 GB | 中〜速い | ◎ | **高精度かつ高速（一押し）** |

> 迷ったら **`large-v3-turbo`**。日本語の精度はほぼ large-v3 と同等で、speed は small と medium の中間。

---

## 8. 初期プロンプトの書き方

Whisper の `initial_prompt` は**指示文ではなく「模範的な書き起こし例」**として書きます。最大 224 トークン（日本語で約 150〜180 文字）。

### 守るべき3つの原則

1. **「指示」を書かない**：「丁寧に書き起こしてください」のようなお願いは無意味です。GPT のプロンプトとは別物。
2. **句読点付きの完成文を 2〜3 文含める**：これにより出力にも句読点が付きます。
3. **固有名詞・専門用語は 10〜15 語に絞る**：多すぎるとハルシネーション（誤生成）の原因になります。

### 良い例（会議録音）

```
本日の経営会議では、第3四半期の業績報告と新製品のリリーススケジュールについて議論しました。
出席者は、山田CEO、佐藤CFO、田中マーケティング部長、開発チームの鈴木リーダーです。
```

### 良い例（医療セミナー）

```
本日は、糖尿病の最新治療法についての勉強会です。
講師は内科の高橋医師、コメンテーターに看護部の伊藤主任が登壇します。
キーワード: HbA1c、インスリン、GLP-1受容体作動薬、SGLT2阻害薬。
```

### 悪い例（やらない方が良い）

- ❌ 「ですます調で書き起こしてください」 ← 指示は効きません
- ❌ 「以下の固有名詞リスト: 名前1、名前2、…(50個)」 ← 多すぎ
- ❌ 200 文字を大きく超える長文 ← トークン上限で切り捨てられます

---

## 9. 入力フォーマットについて

### 受け付ける形式

| 種類 | 拡張子 |
|---|---|
| 音声（非圧縮） | `.wav`（**32bit float WAV にも対応**） |
| 音声（圧縮） | `.mp3` `.m4a` `.aac` `.flac` `.ogg` `.opus` `.wma` |
| 動画 | `.mp4` `.mov` `.mkv` `.avi` `.webm` `.m4v` `.ts` |

### 内部処理フロー

```
入力 (任意形式・複数) ─┐
                      ├─[ffmpeg]→ 各ファイルを 16kHz/mono/pcm_s16le に変換
                      ┘                            │
                                                    ▼
                                  [ffmpeg concat + loudnorm 正規化]
                                                    │
                                                    ▼
                                       最終 WAV (16kHz/mono/16bit)
                                                    │
                                                    ▼
                                          [Whisper.net 推論]
                                                    │
                                                    ▼
                                            .txt / .srt / .vtt
```

### 録音機が 30 分ごとに分割するケース

ファイル名でソートして上から順に投入すれば自動で連結されます。並び順は GUI 上で「上へ／下へ」で調整可能。

---

## 10. 出力フォーマット

### `.txt` — プレーンテキスト

各セグメントを単純に改行で並べたもの。

```
むかしむかしあるところに
おじいさんとおばあさんがおりました
おじいさんは山へしばかりに
...
```

### `.srt` — 字幕（タイムスタンプ付き）

動画字幕用標準フォーマット。

```
1
00:00:04,480 --> 00:00:10,380
むかしむかしあるところに、おじいさんとおばあさんがおりました。

2
00:00:10,880 --> 00:00:14,480
おじいさんは毎日、山へしばかりに
```

### `.vtt` — Web標準字幕

`.srt` とほぼ同じ内容、HTML5 video 用。

---

## 11. ファイル保管場所

| 種別 | パス |
|---|---|
| アプリ本体 | （任意の展開先） |
| ffmpeg / ffprobe | `%LOCALAPPDATA%\WhisperTranscribe\ffmpeg\` |
| Whisper モデル | `%LOCALAPPDATA%\WhisperTranscribe\models\` |
| 中間 WAV ファイル | `%LOCALAPPDATA%\WhisperTranscribe\temp\job_<日時>\` |
| 出力（既定） | `%USERPROFILE%\Documents\WhisperTranscribe\` |

> **温存ファイルの掃除**: `temp\` フォルダは結合済み最終 WAV を残します。ディスク容量が気になる場合は手動で削除して問題ありません。

---

## 12. トラブルシューティング

### Q. アプリ起動直後にネイティブクラッシュ（exit code 0xC0000409 など）

A. GPU ランタイム（Vulkan/CUDA）の自動選択に失敗している可能性があります。**CLI 版を環境変数付きで起動**してください。

```powershell
$env:WHISPER_RUNTIME = "Cpu"
wtcli input.wav --model small
```

または `CpuNoAvx`（古い CPU 向け）も試してください。

GUI 版の場合、`WHISPER_RUNTIME` をシステム環境変数に設定してから再起動します。

### Q. 「ffmpeg.exe が見つかりません」

A. 右上の **「ffmpeg取得」** ボタンを押す、または手動で `%LOCALAPPDATA%\WhisperTranscribe\ffmpeg\` に `ffmpeg.exe` / `ffprobe.exe` を配置します。

### Q. モデルダウンロードが途中で止まる

A. ネットワーク不安定や HuggingFace 側のリトライ等が原因です。`%LOCALAPPDATA%\WhisperTranscribe\models\` に残った `.part` ファイルを削除して再実行してください。

### Q. 文字起こしが極端に遅い

A. 以下を確認:

- **モデルが大きすぎる**: `medium` / `large` 系は CPU だと数倍時間がかかります。`small` か `large-v3-turbo` に下げる。
- **高品質モードが ON**: beam=10 は処理が約2倍重くなります。
- **CPU が古い**: 第4世代 Core より前は AVX2 が無く、`CpuNoAvx` ランタイム経由で動作するため遅い場合があります。

### Q. 出力テキストに句読点がない／変な単語が混ざる

A. **初期プロンプト**を活用してください。[8章](#8-初期プロンプトの書き方)を参照。
特に句読点については、句点を含む完成文を 2〜3 文プロンプトに入れることで改善されます。

### Q. 「32bit float 検出」と出るがそのまま処理できる?

A. はい。ffmpeg が自動で 16bit PCM に変換するため、ユーザー側で何かする必要はありません。

### Q. 大量ファイルを連結すると時間がかかる

A. デコード + concat + loudnorm の前処理はファイル数と総時間に比例します。**ラウドネス正規化が不要な場合は OFF にすると倍速程度になります**（CLI なら `--no-norm`）。

### Q. 進捗バーが動かない区間がある

A. Whisper 内部処理（モデル初期化やセグメント解析の前半）は進捗を返さない区間があります。ログに「文字起こし開始」のメッセージが出ていれば正常に動いています。

---

## 13. Colab 半手動連携（無料 GPU 利用）

NVIDIA GPU を持たない PC でも、Google Colab の無料 T4 GPU を使って高速に文字起こしできます。**CPU 比 5〜15 倍速**。

### 準備（初回のみ）

Google Drive のマイドライブ直下に次のフォルダを作ります（中身は空でOK）:

```
/MyDrive/WhisperTranscribe/inbox/    ← WAV をアップロードする場所
/MyDrive/WhisperTranscribe/outbox/   ← 結果が出力される場所
```

### 使い方

1. **アプリにファイルを追加**（通常通り）
2. 設定パネルで **モデル不要**（Colab 側で選ぶ）、**言語・初期プロンプト**は確認
3. 右上の **「Colab前処理」** ボタンを押す
   - アプリが連結・正規化・16kHz変換を行い、`%USERPROFILE%\Documents\WhisperTranscribe_Colab_Inbox\` に最終 WAV を出力
   - エクスプローラーがそのフォルダを表示し、ブラウザで Colab Notebook が開く
4. **エクスプローラーの WAV を Google Drive の `inbox/` にドラッグ&ドロップ**
5. ブラウザの Colab Notebook で:
   - 上部メニュー: ランタイム → ランタイムのタイプを変更 → **GPU (T4)** を選択
   - 「**4. 設定**」セルを開き、必要なら `MODEL` `LANGUAGE` `INITIAL_PROMPT` を編集
     - アプリの初期プロンプト欄に書いたテキストは、アプリ実行時のダイアログにコピペ可能な形で表示されます
   - メニュー: ランタイム → **すべてのセルを実行** (Ctrl+F9)
   - Drive のマウント承認ダイアログが出たら許可
6. 完了すると `outbox/` に `.txt` `.srt` `.vtt` が生成される（Notebook の最後のセルで PC へ直接ダウンロードもできる）
7. アプリに戻り、右上の **「結果SRT読込」** ボタンから取得した SRT を選択
   - 出力フォルダにコピーされ、文字起こし結果として保存される

### 内部処理フロー

```
[このアプリ] 連結+正規化+16kHz変換
    │
    ▼
[Documents\WhisperTranscribe_Colab_Inbox\*.wav]
    │   (ユーザがドラッグ&ドロップ)
    ▼
[Google Drive /MyDrive/WhisperTranscribe/inbox/]
    │
    ▼
[Colab Notebook on GPU] ─ faster-whisper で文字起こし
    │
    ▼
[Google Drive /MyDrive/WhisperTranscribe/outbox/*.srt]
    │   (ユーザがダウンロード)
    ▼
[このアプリ「結果SRT読込」で出力フォルダへ取り込み]
```

### Notebook の特徴

- **モデル**: 初期値 `large-v3-turbo`（GPU で1分音声 ≒ 6〜10秒）
- **VAD フィルタ**: 無音区間スキップで処理高速化
- **推奨パラメータ適用**: `temperature=0.0`, `condition_on_previous_text=False`（幻覚抑制）
- **対応形式**: WAV/MP3/M4A/FLAC（アプリが WAV を吐くので通常 WAV）

### Colab 無料枠の制限

| 項目 | 制限 |
|---|---|
| 連続セッション | 最大 12 時間 |
| アイドル切断 | 約 90 分操作なしで切断 |
| GPU 種別 | T4 が割当（混雑時は CPU のみになることも） |
| 月間利用上限 | 厳格な数値非公開、ヘビーユーザーには制限 |

> Notebook の最初のセル `!nvidia-smi` の出力で T4 が割り当てられたか確認できます。「No GPU found」の場合はランタイムタイプ変更からGPU選択してください。

### よくある質問

**Q. Drive にアップロードした WAV が Notebook から見えない**

A. マイドライブの **直下** に `WhisperTranscribe` フォルダが必要です。`/MyDrive/Apps/...` などにあると Notebook の探索パスと合いません。

**Q. 「No CUDA GPUs are available」エラー**

A. ランタイム → ランタイムのタイプを変更 で GPU を選び、ランタイム → ランタイムを再起動して再実行。

**Q. メモリ不足エラー (`large-v3` でクラッシュ)**

A. 設定セルで `COMPUTE_TYPE = 'int8_float16'` に変更すると VRAM 使用量が半減。または `MODEL = 'large-v3-turbo'`（既定）の方が軽量で実用的。

**Q. ファイルが大きすぎてアップロードが遅い**

A. ラウドネス正規化済み 16kHz mono WAV は 1 時間で約 110 MB。長尺ならアプリ側で分割してから upload するのも有効。

---

## 付録: ライセンス

- 本アプリ: MIT
- whisper.cpp / Whisper.net: MIT
- ffmpeg: LGPL/GPL（バイナリ同梱せず、初回起動時に gyan.dev から自動取得）

リポジトリ: <https://github.com/NamakeroBK/WhisperTranscribe>
